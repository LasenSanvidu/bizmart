name: myapp
description: "Learn"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts:
  go_router: ^12.1.1
  firebase_core: ^2.32.0
  lottie:


dev_dependencies:
  flutter_test:
    sdk: flutter
  cloud_firestore: ^4.13.0 # Use the latest version
  cached_network_image: ^3.3.0

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Adding asset directory for images
  assets:
    - assets/
    - assets/ring_o.png
    - assets/userType.jpg
    - assets/BUser.jpg
    - lib/Icons/
    - lib/animations/
